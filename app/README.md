# RAG Demo – Flutter app

Chat UI that calls the Python RAG API. See the [root README](../README.md) for setup and how to run the API first.

- **Run:** `flutter pub get` then `flutter run`
- **API URL:** Edit `_kRagBaseUrl` in `lib/main.dart` (use `http://10.0.2.2:8000` on Android emulator)
